import { StudentDailySchedule } from './student-daily-schedule';

describe('StudentDailySchedule', () => {
  it('should create an instance', () => {
    expect(new StudentDailySchedule()).toBeTruthy();
  });
});
