import { DocumentsPerTaskExsist } from './documents-per-task-exsist';

describe('DocumentsPerTaskExsist', () => {
  it('should create an instance', () => {
    expect(new DocumentsPerTaskExsist()).toBeTruthy();
  });
});
