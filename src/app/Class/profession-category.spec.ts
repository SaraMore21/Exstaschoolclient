import { ProfessionCategory } from './profession-category';

describe('ProfessionCategory', () => {
  it('should create an instance', () => {
    expect(new ProfessionCategory()).toBeTruthy();
  });
});
