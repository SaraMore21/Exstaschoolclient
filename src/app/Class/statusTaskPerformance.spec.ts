import { StatusTaskPerformance } from './statusTaskPerformance';

describe('StatusTaskPerformance', () => {
  it('should create an instance', () => {
    expect(new StatusTaskPerformance()).toBeTruthy();
  });
});
