import { ContactPerStudent } from './contact-per-student';

describe('ContactPerStudent', () => {
  it('should create an instance', () => {
    expect(new ContactPerStudent()).toBeTruthy();
  });
});
