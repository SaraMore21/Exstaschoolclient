import { DocumentsPerGroup } from './documents-per-group';

describe('DocumentsPerGroup', () => {
  it('should create an instance', () => {
    expect(new DocumentsPerGroup()).toBeTruthy();
  });
});
