import { DocumentsPerCourse } from './documents-per-course';

describe('DocumentsPerCourse', () => {
  it('should create an instance', () => {
    expect(new DocumentsPerCourse()).toBeTruthy();
  });
});
