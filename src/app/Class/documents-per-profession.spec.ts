import { DocumentsPerProfession } from './documents-per-profession';

describe('DocumentsPerProfession', () => {
  it('should create an instance', () => {
    expect(new DocumentsPerProfession()).toBeTruthy();
  });
});
