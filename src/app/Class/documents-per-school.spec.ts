import { DocumentsPerSchool } from './documents-per-school';

describe('DocumentsPerSchool', () => {
  it('should create an instance', () => {
    expect(new DocumentsPerSchool()).toBeTruthy();
  });
});
