import { TypeAttendanceMarking } from './type-attendance-marking';

describe('TypeAttendanceMarking', () => {
  it('should create an instance', () => {
    expect(new TypeAttendanceMarking()).toBeTruthy();
  });
});
